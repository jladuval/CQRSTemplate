@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <title>CQRS Cruise Ship</title>
    <style type="text/css">
        .container {
            background-color: #99CCFF;
            border: thick solid #808080;
            padding: 20px;
            margin: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Create a large, complicated cruise ship</h2>
        <input type="text" id="name" />
        <input type="button" id="createShip" value="Create" />
        <ul id="discussion">
        </ul>
    </div>
    <!--Script references. -->
    <!--Reference the jQuery library. -->
    <script src="~/Scripts/jquery-1.9.1.min.js"></script>
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-1.1.3.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="/signalr/hubs"></script>
    <!--Add script to update the page and send messages.--> 
    <script type="text/javascript">
        $(function () {
            
            // Declare a proxy to reference the hub. 
            var chat = $.connection.eventHub;
            // Create a function that the hub can call to broadcast messages.
            chat.client.eventStatus = function (name) {
                // Html encode display name and message. 
                var encodedMsg = $('<div />').text(name).html();
                // Add the message to the page. 
                $('#discussion').append('<li><strong>Update:</strong>:&nbsp;&nbsp;' + encodedMsg + '</li>');
            };
            // Set initial focus to message input box.  
            $('#name').focus();
            // Start the connection.
            $.connection.hub.start({ transport: 'longPolling' }, function () {
                console.log('connection started!');
            }).done(function () {
                $('#createShip').click(function () {
                    $.post('/Home/CreateCruiseShip', { name: $('#name').val() });
                    // Call the Send method on the hub. 
                    $('#name').val('').focus();
                });
            });
        });
    </script>
</body>
</html>